import { parse, parseISO } from "date-fns"
import { PostLayout } from "../../components/PostLayout"
import { ImageWrapper } from "../../components/atoms/ImageWrapper"

export const meta = {
  title: "ブログにreact-native-skiaを導入しました",
  tagNames: ["react-native", "blog", "react-native-skia", "skia"],
  color1: "#4CC8F0",
  color2: "#3B8FFD",
  createdAt: parse(
    "2022-08-04 11:45:00 +09:00",
    "yyyy-MM-dd HH:mm:ss XXX",
    new Date(),
  ),
  author: "@gaishimo",
  description: "当ブログにreact-native-skiaを導入しました",
  imagePath: null,
  lastUpdatedAt: parse(
    "2022-08-04 11:45:00 +09:00",
    "yyyy-MM-dd HH:mm:ss XXX",
    new Date(),
  ),
}

export const headlines = [{ title: "" }]

export default props => (
  <PostLayout headlines={headlines} meta={meta}>
    {props.children}
  </PostLayout>
)
