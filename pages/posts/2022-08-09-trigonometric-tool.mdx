import { parse, parseISO } from "date-fns"
import { PostLayout } from "../../components/PostLayout"
import { ImageWrapper } from "../../components/atoms/ImageWrapper"
import { ReanimatedSample } from "../../components/pages/2022-08-08-reanimated-web-config/ReanimatedSample"
import { View, Text, ActivityIndicator } from "react-native"
import { WithSkiaWeb } from "@shopify/react-native-skia/lib/module/web"
import { meta } from "../../posts-meta/2022-08-09-trigonometric-tool"
import { WithSkia } from "../../components/WithSkia"

export const headlines = [{ title: "コード" }]

export default props => (
  <PostLayout headlines={headlines} meta={meta}>
    {props.children}
  </PostLayout>
)


三角関数のsin,cosを計算するツールです。下の入力値を変更すると半径、角度の値を変更することができます。


<View style={{ marginBottom: 16 }}>
  <WithSkia
    getComponent={() =>
      import("../../components/pages/2022-08-09-trigonometric-tool/TrigonometricTool")
    }
    fallback={() => <LoadingRect width={300} height={300} textColor="lightblue" bgColor={"rgb(240, 240, 240)"} />}
  />
</View>

## コード

図の部分はreact-native-skiaで描画しています。コードは以下で確認することが可能です。

[omoidasu-tech-blog/TrigonometricTool.tsx at main · gaishimo/omoidasu-tech-blog](https://github.com/gaishimo/omoidasu-tech-blog/blob/main/components/pages/2022-08-09-trigonometric-tool/TrigonometricTool.tsx)

今後、ドラッグで角度を変更できるようにしたり等していこうかと思います。
